name: Deploy to production

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H 161.104.35.240 >> ~/.ssh/known_hosts
          ssh -i ~/.ssh/deploy_key root@161.104.35.240 "cd /opt/miniteatree && git pull && docker compose up -d --build app"
